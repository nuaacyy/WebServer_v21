添加对重定向功能的支持
重定向的两个关键点在于服务端响应客户端时的状态码以及一个响应头

状态代码：302表示要求客户端重新发起请求访问给定的地址
响应头：Location,这个头就是告诉客户端浏览器要重定向的地址

为此，我们要修改HttpResponse的内容，将发送状态行的代码进行修改
对不同状态代码发送进行支持
然后在HttpResponse中提供对重定向功能支持的方法

在httpcontext中添加一个map，用于记录不同的状态代码所对应的状态描述，在添加‘
若干常量 定义状态代码
